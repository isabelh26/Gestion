spring:
  datasource:
    url: jdbc:postgresql://localhost:30001/gestion
    username: admin
    password: admin01
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

server:
  port: 8080
